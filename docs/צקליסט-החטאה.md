---
related_files:
  - src/screens/MissChecklistScreen.tsx
  - src/screens/MissChecklistListScreen.tsx
  - src/screens/missChecklistForm.schema.ts
  - src/domain/missChecklist.types.ts
  - src/domain/missChecklist.ts
  - src/storage/missChecklistStorage.ts
  - src/useCases/addMissChecklist.ts
---

# צ'קליסט החטאה

## תיאור
- טופס לתיעוד בדיקות אחרי החטאה.
- כל צ'קליסט נשמר כרשומה נפרדת עם זמן עדכון אחרון.
- רשימת הצ'קליסטים ממוינת לפי זמן העדכון האחרון — המעודכן ביותר מופיע ראשון.
- הנתונים נשמרים מקומית בלבד ולא נשלחים החוצה.

## חוקי חובה ושמירה
- אין כרגע שדות חובה בטופס.
- אפשר לשמור גם טופס חלקי או ריק.
- שדות בחירה מסוג כן/לא מקבלים ברירת מחדל: לא.
- שדות בחירה אחרים מקבלים את ברירת המחדל שמצוינת לידם.

## סוגי שדות
- **טקסט** — ערך חופשי.
- **מספר** — ערך מספרי.
- **בחירה** — בחירה מתוך אפשרויות קבועות.

- **זיהוי מקום הפגיעה** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **תשובה** — מופיע בתוך הכרטיס; בחירה: כן, לא; ברירת מחדל: לא.
  - אם **כן** — נפתחת בחירת סוג מיקום: קרוב, רחוק, תיאור מילולי, נ.צ.
  - **קרוב** / **רחוק** — אין שדות נוספים; הבחירה עצמה נשמרת.
  - **תיאור מילולי** — טקסט חופשי, אופציונלי.
  - **נ.צ.** — הזנת נ.צ. (מזרח, צפון, פלח), אופציונלי.
  - **האם התפוצץ** — בחירה: כן, לא; ברירת מחדל: לא.
  - אם **לא** — שדות המיקום לא מוצגים.
- **השוואה מול מציין של נתוני המטרה עם הצ'קליסט** — תיבת סימון; ברירת מחדל: לא מסומן.
- **בדיקת הקוץ שמוזן באמצעי מול מציין** — תיבת סימון מתחת לשדה ההשוואה; מוצגת רק אם לא זוהה מקום הפגיעה, או שזוהה מקום אך התשובה על "האם התפוצץ" היא לא; ברירת מחדל: לא מסומן.
- **האם היציאה הייתה תקינה** — בחירה: כן, לא; מוצגת מתחת לבדיקת הקוץ, רק כשסוג מיקום הפגיעה הוא רחוק, תיאור מילולי או נ.צ. (לא כשקרוב); ברירת מחדל: לא.
  - אם **לא** — נפתחים שני שדות נוספים:
  - **פרט** — בחירה מרובה (מעוף, רעש, ריח, עשן); אפשר לסמן כמה שרוצים; אופציונלי.
  - **פירוט נוסף** — טקסט חופשי, אופציונלי.
- **וידוא שם המצין וקוד הציון תואמים למשימה במחשב** — תיבת סימון; ברירת מחדל: לא מסומן.
- **בדיקת תנאי מז"א בקרבת המטרה עם המציין** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **רוח**, **עננים או אובך**, **גשם** — מופיעים בתוך כרטיס תיבת הסימון; בחירה: כן, לא; ברירת מחדל: לא לכל אחד.
  - מסגרת הכרטיס מודגשת באדום אם לפחות אחד מ**רוח**, **עננים או אובך** או **גשם** הוא **כן**.
- **בדיקת תנאי מז"א בגובה המעוף עם תא תקיפה** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **תקין** — מופיע בתוך כרטיס תיבת הסימון; בחירה: כן, לא; ברירת מחדל: לא.
  - מסגרת הכרטיס מודגשת באדום כל עוד **תקין** הוא **לא**.
- **העברת אמורה למציין ושאילת זווית קודקוד** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **זווית קודקוד** — מופיע בתוך כרטיס תיבת הסימון; מספר, אופציונלי.
  - מסגרת הכרטיס מודגשת באדום כש**זווית קודקוד** גדולה מ-55; אז מופיעה גם הודעה: **\*הערך מעל 55**.
- **הסתרים בקרבת המטרה (שאילה חוזרת למציין)** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **יש הסתרים?** — מופיע בתוך כרטיס תיבת הסימון; בחירה: יש, אין; ברירת מחדל: אין.
  - אם **יש** — מופיעה תיבת סימון **חשב מחדש**; ברירת מחדל: לא מסומן.
  - מסגרת הכרטיס מודגשת באדום כש**יש הסתרים?** הוא **יש**.
- **וידוא שאין כפילות במספר מטרה ו/או משימה (סינון)** — תיבת סימון; ברירת מחדל: לא מסומן.
- **אופן הזנת האק"א** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **אופן ההזנה** — מופיע בתוך כרטיס תיבת הסימון; בחירה: מוצא צפון, טימאפס, מצפן; ברירת מחדל: מוצא צפון.
  - **בדוק אק"א מחדש** — מופיע בתוך הכרטיס רק כש**אופן ההזנה** הוא **מצפן**, וגם: לא זוהה מקום הפגיעה, או שזוהה מקום והמיקום הוא **רחוק**; תיבת סימון אופציונלית; ברירת מחדל: לא מסומן. לא מוצג עבור מוצא צפון או טימאפס, ולא כשזוהה מקום והמיקום הוא קרוב, תיאור מילולי או נ.צ.
- **הזנת נתוני משימה** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **טווח (מטרים)** — מספר, אופציונלי.
  - **הפרש גובה** — מספר, אופציונלי.
  - **מסלול** — בחירה: +lofted, lofted, low, flat; אופציונלי.
- **ירי בכיווניות** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **האם היה ירי בכיווניות?** — מופיע בתוך כרטיס תיבת הסימון; בחירה: כן, לא; ברירת מחדל: לא.
  - מסגרת הכרטיס מודגשת באדום כש**האם היה ירי בכיווניות?** הוא **כן** וגם **טווח (מטרים)** מהזנת נתוני המשימה גדול מ-8000; אז מופיעה גם הודעה שמסבירה למה.
  - **בדוק מחדש מכשולים והסתרים מכיוון מעוף הטיל** — מופיע בתוך הכרטיס רק אם התשובה **כן**; תיבת סימון אופציונלית; ברירת מחדל: לא מסומן.
- **סוג המטרה** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **סוג המטרה** — מופיע בתוך כרטיס תיבת הסימון; טקסט, אופציונלי.
- **מטרה רפלקטיבית או משטחים רפלקטיביים בקרבתה** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **תשובה** — מופיע בתוך הכרטיס; בחירה: כן, לא; ברירת מחדל: לא.
  - מסגרת הכרטיס מודגשת באדום כש**תשובה** היא **כן**.
- **מפנה המטרה** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **מפנה המטרה** — מופיע בתוך הכרטיס; טקסט, אופציונלי.
- **ריבוי כתמים** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **תשובה** — מופיע בתוך הכרטיס; בחירה: כן, לא; ברירת מחדל: לא.
  - מסגרת הכרטיס מודגשת באדום כש**תשובה** היא **כן**.
- **הקפצת כתם** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **תשובה** — מופיע בתוך הכרטיס; בחירה: כן, לא; ברירת מחדל: לא.
  - מסגרת הכרטיס מודגשת באדום כש**תשובה** היא **כן**.
- **הסטה** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **תשובה** — מופיע בתוך הכרטיס; בחירה: כן, לא; ברירת מחדל: לא.
- **מיקום הצלב ביחס לנקודת הפגיעה הרצויה** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **מיקום הצלב** — מופיע בתוך הכרטיס; טקסט, אופציונלי.
- **מיקום פגיעת הטיל ביחס לצלב** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **מיקום הפגיעה** — מופיע בתוך הכרטיס; טקסט, אופציונלי.
- **גלישה של הכתם** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **תשובה** — מופיע בתוך הכרטיס; בחירה: כן, לא; ברירת מחדל: לא.
  - מסגרת הכרטיס מודגשת באדום כש**תשובה** היא **כן**.
- **גודל הכתם (כולל מריחה)** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **גודל הכתם (כולל מריחה)** — מופיע בתוך הכרטיס; טקסט, אופציונלי.
- **בליעה של הכתם** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **תשובה** — מופיע בתוך הכרטיס; בחירה: כן, לא; ברירת מחדל: לא.
  - מסגרת הכרטיס מודגשת באדום כש**תשובה** היא **כן**.

## שיגור
- **תקינות מצייני הנפילה בהוצאת הטילים מהמזוודות** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **תשובה** — מופיע בתוך הכרטיס; בחירה: כן, לא; ברירת מחדל: לא.
  - מסגרת הכרטיס מודגשת באדום כש**תשובה** היא **לא**.
- **תקינות הכוורת** — תיבת סימון; ברירת מחדל: לא מסומן.
  - **תקין?** — מופיע בתוך הכרטיס; בחירה: כן, לא; ברירת מחדל: לא.
  - מסגרת הכרטיס מודגשת באדום כש**תקין?** הוא **לא**.

## שמירה מקומית
- הצ'קליסטים נשמרים במכשיר ונשארים לאחר סגירה ופתיחה מחדש של האפליקציה.
- ממסך רשימת הצ'קליסטים: לחיצה ארוכה על כרטיס פותחת תפריט; משם ניתן למחוק. המחיקה דורשת אישור, מסירה את הרשומה מהמכשיר ולא ניתן לשחזר מתוך האפליקציה.
